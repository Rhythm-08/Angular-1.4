import { MyBackgroundDirective } from './my-background.directive';

describe('MyBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MyBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
